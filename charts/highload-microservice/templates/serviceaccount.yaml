{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "highload-microservice.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "highload-microservice.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}

